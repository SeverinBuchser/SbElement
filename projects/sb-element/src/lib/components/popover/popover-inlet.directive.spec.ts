import { PopoverInletDirective } from './popover-inlet.directive';

describe('PopoverInletDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverInletDirective();
    expect(directive).toBeTruthy();
  });
});
