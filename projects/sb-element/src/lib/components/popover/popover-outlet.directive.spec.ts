import { PopoverOutletDirective } from './popover-outlet.directive';

describe('PopoverOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new PopoverOutletDirective();
    expect(directive).toBeTruthy();
  });
});
