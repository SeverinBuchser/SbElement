import { SelectionOptionsDirective } from './selection-options.directive';

describe('SelectionOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
