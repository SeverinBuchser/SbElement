import { PopupTriggerClickDirective } from './popup-trigger-click.directive';

describe('PopupTriggerClickDirective', () => {
  it('should create an instance', () => {
    const directive = new PopupTriggerClickDirective();
    expect(directive).toBeTruthy();
  });
});
