import { PopperTriggerMouseoverDirective } from './popper-trigger-mouseover.directive';

describe('PopperTriggerMouseoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PopperTriggerMouseoverDirective();
    expect(directive).toBeTruthy();
  });
});
