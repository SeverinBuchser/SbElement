import { HoverTriggerDirective } from './hover-trigger.directive';

describe('HoverTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
