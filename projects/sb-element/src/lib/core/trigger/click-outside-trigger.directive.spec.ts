import { ClickOutsideTriggerDirective } from './click-outside-trigger.directive';

describe('ClickOutsideTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsideTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
