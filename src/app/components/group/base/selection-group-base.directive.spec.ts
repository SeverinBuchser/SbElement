import { SelectionGroupBaseDirective } from './selection-group-base.directive';

describe('SelectionGroupBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionGroupBaseDirective();
    expect(directive).toBeTruthy();
  });
});
