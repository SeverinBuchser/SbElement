import { FormGroupBase } from './form-group.base.directive';

describe('FormGroupBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupBase();
    expect(directive).toBeTruthy();
  });
});
