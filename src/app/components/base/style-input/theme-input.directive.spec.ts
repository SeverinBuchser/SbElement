import { ThemeInputDirective } from './theme-input.directive';

describe('ThemeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeInputDirective();
    expect(directive).toBeTruthy();
  });
});
