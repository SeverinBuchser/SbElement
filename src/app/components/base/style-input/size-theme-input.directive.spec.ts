import { SizeThemeInputDirective } from './size-theme-input.directive';

describe('SizeThemeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SizeThemeInputDirective();
    expect(directive).toBeTruthy();
  });
});
