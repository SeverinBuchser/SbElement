import { StateManagerDirective } from './state-manager.directive';

describe('StateManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new StateManagerDirective();
    expect(directive).toBeTruthy();
  });
});
