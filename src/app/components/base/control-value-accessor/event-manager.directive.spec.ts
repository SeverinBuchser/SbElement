import { EventManagerDirective } from './event-manager.directive';

describe('EventManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new EventManagerDirective();
    expect(directive).toBeTruthy();
  });
});
